# Software-Testing-API-
STEP 1: ติดตั้ง Playwright

npm init playwright@latest
เลือก:
JavaScript ✅
Yes to install browsers
Yes to create example tests

STEP 2: สร้างไฟล์ทดสอบ
tests/
สร้างไฟล์ [ชื่อไฟล์].spec.js

วิธีรันทั้งหมด
npx playwright test

ถ้าจะรันเฉพาะไฟล์:
npx playwright test tests/[ชื่อไฟล์].spec.js
